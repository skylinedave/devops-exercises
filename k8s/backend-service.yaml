apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: devops
spec:
  type: NodePort
  selector:
    app: backend
  ports:
    - name: http
      port: 80
      targetPort: 5678
      nodePort: 30082
